# azure-pipelines-infra.yml

trigger: none # Manual trigger only

pool:
  vmImage: 'ubuntu-latest' # Use a hosted agent

variables:
- group: 'perf-demo-vars'
- name: tfstate_key
  value: '$(resource_group_name)-$(aks_cluster_name).tfstate'
- name: ARM_USE_CLI
  value: false

stages:
- stage: ProvisionInfrastructure
  displayName: 'Provision Infrastructure with Terraform'
  jobs:
  - job: TerraformApply
    displayName: 'Terraform Apply'
    steps:
    - checkout: self # Checks out the monorepo

    # Step 1: Set SQL credentials as pipeline variables
    - task: AzureCLI@2
      displayName: 'Set Azure CLI context'
      inputs:
        azureSubscription: 'aks-demo-service-connection'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          echo "##vso[task.setvariable variable=SQL_PASSWORD;isSecret=true]$(SQL_PASSWORD)"
          echo "##vso[task.setvariable variable=SQL_USERNAME;isSecret=true]$(SQL_USERNAME)"
          
    # Step 2: Expose Service Principal credentials for Terraform
    - task: AzureCLI@2
      displayName: 'Expose Service Principal to Environment'
      inputs:
        azureSubscription: 'aks-demo-service-connection'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        addSpnToEnvironment: true # This is the crucial line for Terraform authentication
        inlineScript: |
          echo "Service Principal credentials are now available as environment variables for Terraform."
          
    # Step 3: Install Terraform CLI
    - task: TerraformInstaller@0
      displayName: 'Install Terraform CLI'
      inputs:
        terraformVersion: '1.2.9'

    - task: Bash@3
      displayName: 'Install Terraform'
      inputs:
        targetType: 'inline'
        script: |
          curl -sL https://releases.hashicorp.com/terraform/1.2.9/terraform_1.2.9_linux_amd64.zip -o terraform.zip
          unzip terraform.zip
          sudo mv terraform /usr/local/bin/

    - task: AzureCLI@2
      displayName: 'Terraform Init'
      inputs:
        azureSubscription: 'aks-demo-service-connection'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        addSpnToEnvironment: true
        inlineScript: |
          terraform -chdir=infra init \
            -backend-config="resource_group_name=terraform-backend-rg" \
            -backend-config="storage_account_name=aksdemotfstate001" \
            -backend-config="container_name=aksdemotfstate" \
            -backend-config="key=$(tfstate_key)"

    - task: TerraformTaskV4@4
      displayName: 'Terraform Validate'
      inputs:
        provider: 'azurerm'
        command: 'validate'
        workingDirectory: 'infra'

    - task: TerraformTaskV4@4
      displayName: 'Terraform Format'
      inputs:
        provider: 'azurerm'
        command: 'fmt'
        workingDirectory: 'infra'

    - task: TerraformTaskV4@4
      displayName: 'Terraform Plan'
      inputs:
        provider: 'azurerm'
        command: 'plan'
        workingDirectory: 'infra'
        environmentServiceName: 'aks-demo-service-connection'
          
    - task: TerraformTaskV4@4
      displayName: 'Terraform Plan'
      inputs:
        provider: 'azurerm'
        command: 'plan'
        workingDirectory: 'infra'
        environmentServiceName: 'aks-demo-service-connection' # Use your service connection name here
        
    - task: TerraformTaskV4@4
      displayName: 'Terraform Apply'
      inputs:
        provider: 'azurerm'
        command: 'apply'
        workingDirectory: 'infra'
        environmentServiceName: 'aks-demo-service-connection' # Use your service connection name here